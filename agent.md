# agent.md — 模型配合模式（交互式调度协议 · 正文）

> 这是项目级的**交互协议**，不是说明文档。
> 核心思路：难任务用强模型、简单任务用便宜模型，把成本意识做成一套每次开工都要过的调度流程。
> 配套：[`rubric.md`](./rubric.md) — 9 维难度量表，**只在定级这一步加载**。
> ⚠️ 本文件是**按需加载的正文**，不在每一轮自动注入——入口见 `~/.codex/AGENTS.md`。

---

## 0. 开场协议：新会话第一条回复，无条件触发，且一次只问一个问题

> **为什么强调"无条件"**：实测中模型收到"你好"时会自行判断"还没开工"而跳过询问，
> 用户就看不到模式入口了。所以规则写死：只要是新会话的第一条回复，就必须先问配置。

**强制规则：新会话的第一条回复，无论用户说了什么（打招呼、闲聊、提问都算），只输出【开工配置 1/3】，然后停下等回答。**

**铁律：一次只问一个问题，用户回答后才能问下一个。禁止把 ②③ 和 ① 捆在一起，禁止先回答问题或寒暄。**
唯一例外：用户明确说"不用调度模式／直接做"。

### 逐问流程

**第 1 问**（新会话第一条回复只输出这个）：
```
【开工配置 1/3】
本次是否进入模型配合模式？
进入后：任务会先拆分、按难度分派给不同成本的模型，能并行的并行执行。
[ 进入 ]   [ 本次不用，直接做 ]
```
答"进入" → 问第 2 问；答"不用" → 正常干活，本轮不再提示。

**第 2 问**：
```
【开工配置 2/3】
拆分颗粒度选哪个？
粗 3–5 个 ｜ 中 6–10 个（默认）｜ 细 11–20 个 ｜ 自动（我按任务规模定）
```

**第 3 问**（先探测再问，**不要预设模型名**）：

> ⚠️ **不要假设用户用的是哪家模型。** 强档是他当前会话的模型（可能是 DeepSeek、GPT、Claude 任意一家），
> 弱档更是五花八门。第 3 问必须先探测；探测不到就**主动问**——尤其第一次启用时，
> 用户很可能压根没配弱模型 API。这时默默降级成"全部我来做"是错的，会把省钱的大头悄悄吞掉。

**先探测**（不要跳过）：

```bash
# 强档：读 Codex 配置里当前的默认模型——它就是强档
grep -m1 '^model' ~/.codex/config.toml

# 弱档：看有没有可用的 API（环境变量 或 config 里的 provider）
env | grep -iE 'API_KEY' | sed 's/=.*/=<set>/'
grep -A3 'model_providers' ~/.codex/config.toml | grep -E 'base_url|env_key|bearer_token'
```

**情况 A：弱档 API 可用** → 直接确认

```
【开工配置 3/3】
强档（接 L4/L5）：<探测到的当前会话模型>
弱档（接 L1/L2）：<探测到的弱档模型>
[ 沿用上次 ]   [ 换成：____ ]
```

**情况 B：没有弱档 API（第一次启用最常见）** → 主动询问，不许默默降级

```
【开工配置 3/3】
强档（接 L4/L5）：<当前会话模型>，已就绪

⚠️ 没检测到可用的弱模型 API。
   缺了它，改名、补注释这类机械活也只能我（强档）来做，
   成本和耗时会明显上升（实测弱档能省 87%）。

你有可用的便宜模型 API 吗？
   1) 火山方舟 GLM（ark.cn-beijing.volces.com）
   2) DeepSeek（api.deepseek.com）
   3) 其他 —— 告诉我端点和环境变量名
   （1、2 只是举例，任何提供 Chat Completions 的厂商都行）
   4) 本轮不用弱档，全部我做

选 1–3 的话，把 API Key 给我（或者说它存在哪个环境变量里），
我写入 ~/.codex/config.toml（权限 600）后用 canary 探测验证。
```

**要点**：
- **强档永远存在**（就是当前会话模型），不需要用户提供。
- **弱档缺失时必须问**，不能自作主张跳过——这是整套方案省钱的关键一环。
- 拿到 key 后写入 config，并用 canary 探测确认真的能通，别写完就算数。

**第 3 问确认后**：
- 配置**写回** `.route-state.json`（密钥禁止入内）。
- 用户消息里**已含具体任务** → 输出【任务分配方案】（见下）。
- 没有任务 → 回复"配置完成，把任务发我"。

### 第 4 步：接到具体任务后，先展示方案再动手

**不允许直接开干**，必须先输出方案：
（下面表格里的模型名是**示例**，实际填你第 3 问探测到的）

```
【任务分配方案】

任务：<用户原话>
颗粒度：中

| # | 子任务 | 难度分 | 等级 | 路由模型 | 并行组 | 依赖 |
|---|--------|--------|------|----------|--------|------|
| 1 | 把 utils.ts 里 3 个函数改驼峰 | 14 | L1 | glm-5-2-260617 | G1 | -   |
| 2 | 补 foo() 的 JSDoc              | 11 | L1 | glm-5-2-260617 | G1 | -   |
| 3 | 为 AuthService 写单测          | 30 | L2 | glm-5-2-260617 | G1 | -   |
| 4 | 修登录态跨模块 bug             | 47 | L3 | 升给强档        | G2 | 1,3 |
| 5 | 改 session 接口契约            | 68 | L4 | deepseek-v4-pro | G3 | 4   |

汇总：5 个子任务 ｜ 强 1 ｜ 中 1 ｜ 弱 3 ｜ 并行组 3
预计：G1 三任务并行 ≈ 单次调用耗时；G2、G3 串行
兜底预案：弱模型失败 → 重试1次 → 串行重试 → 升档 → 强档接手

[ 执行 ]   [ 调整：____ ]   [ 只要方案，先不动 ]
```

用户说"执行"才动。说"调整"就按意见改方案再问一次。

### 0.5 非交互模式（`codex exec` / CI / 脚本调用）

> **实测发现**：`codex exec` 是一次性执行、没有用户可以等待，模型会直接跳过四问开干。
> 所以非交互模式必须换一套约定，否则整套协议形同虚设。

1. **不询问，但必须声明配置。** 输出开头固定写：
```
【本次配置】来自 .route-state.json
   模型配合模式 ｜ 颗粒度：中
   强档：<当前会话模型> ｜ 弱档：<探测到的弱档模型>
   （要改：编辑 .route-state.json，或改用交互模式重跑）
```
2. **分配方案表格照常输出**，一步都不能省（表格格式同「第 4 步」）。
3. **是否执行，按风险自动判断**：

| 方案内容 | 行为 |
|---|---|
| 最高等级 ≤ L3 | 输出方案后**直接执行**，收尾给报告 |
| 含 L4 / L5 | **只输出方案，不执行**，提示需确认后重跑 |
| 含任何不可逆操作（D4 = 4） | **只输出方案，不执行**，同上 |

4. 交互模式（TUI 实时对话）才完整走四问——非交互模式下，用户敲下命令即视为已授权。

---

## 1. 状态持久化

每次配置确认后写入项目根的 `.route-state.json`（下面是**示例结构**，值以你的实际配置为准）：

```json
{
  "mode_on": true,
  "granularity": "中",
  "strong_model": "deepseek-v4-pro",
  "strong_endpoint": "https://api.deepseek.com/v1",
  "cheap_model": "glm-5-2-260617",
  "cheap_endpoint": "https://ark.cn-beijing.volces.com/api/v3",
  "cheap_no_think": true,
  "mid_model": null,
  "last_updated": "2026-09-08"
}
```

这个文件要**提交进仓库**（团队共享同一套默认值），但**密钥禁止写进去**，只写模型名和端点地址。

---

## 2. 模型分工与通道

| 档位 | 承接 | 当前配置 | 通道 | 协议 |
|---|---|---|---|---|
| **S 强** | L4 / L5 | **DeepSeek v4-pro** | Codex 主会话，带完整工具权限 | Responses |
| **M 中** | L3 | 未配置 → 升给 S（但见下方下放规则） | — | — |

**L3 编码任务可以下放给弱档**——实测 9 道编程题，弱档 8/9、强档 8/9，正确率持平，
但弱档快 2.7 倍、省 87%。下放条件：**单函数/单类 + 输入输出明确 + 上下文能一次贴全**。
（不下的：跨文件协调、需要设计决策、上下文要它自己探索。详见 `rubric.md` §六。）
| **W 弱** | L1 / L2 | **GLM-5.2（火山方舟）** | 直连 HTTP，不经 Codex | Chat Completions |

> **为什么 W 档必须绕开 Codex**：火山方舟只提供 Chat Completions，而 Codex 只说 Responses 协议，
> 接进去会直接 404/空流。绕开既省钱，也顺带绕开了协议限制。
> 当前值记在 `.route-state.json`，改模型只动那一个文件。

> ⚠️ **S 档就是你自己**（config.toml 的默认模型），由主会话直接承担——
> **不需要检查任何环境变量，不需要 shell 调用，不存在"强档密钥未配置"这回事**。
> 实测踩过：桌面端把"S 档"误判为需要 shell 调 API，检查 `DEEPSEEK_API_KEY` 后错误地宣布密钥缺失、全量转兜底。
> 判定"通道不可用"之前，先想清楚：只有 **W 档**才需要密钥（GLM 的 key 会自动从 `config.toml` 的 bearer_token 回退读取，见 `weak-channel.md`）。

### 2.1 当前选定的模型

> ⚠️ **下表是示例值**（本项目实测通过的一组搭配）。你的实际配置以 `.route-state.json` 为准——
> 第 3 问探测到什么就是什么，**不要照抄这张表**。换任何一家厂商都不影响规则本身。

| 档 | 模型（示例） | 厂商端点（示例） | 说明 |
|---|---|---|---|
| **S 强** | `deepseek-v4-pro` | `https://api.deepseek.com/v1` | 实测跑通；换 GPT / Claude 同理 |
| **M 中** | 未配置 | — | L3 编码任务可下放弱档（见 §2 下放规则） |
| **W 弱** | `glm-5-2-260617` | `https://ark.cn-beijing.volces.com/api/v3` | 实测跑通；**推理模型须关思考** |

**W 档的三条硬约束**（违反即视为路由失败）：
1. 只做纯文本变换，输入必须贴全（把相关代码/文本直接放进 prompt），不许它碰文件系统。
2. `temperature = 0`，输出格式在 prompt 里写死（"只输出代码，不要解释，不要 markdown 围栏"）。
3. 单次调用必须能独立完成——**它反问 = 任务没拆干净，退回重拆，不许让它猜**。

> 注意：需要读写文件或执行命令的子任务，**最低 L3**（见 rubric G1 门槛），弱模型接不了。

**W 档的大文件陷阱**（实测踩到）：
一个子任务就算定级 L1（比如"把 300 行文件里 5 处 L1 改成 Level 1"），
也**不要把整个文件塞给弱模型让它重写一遍**——它很可能顺手改掉别的地方，这种漂移比慢一点贵得多。

正确做法二选一：
1. **精确片段替换**：只贴涉及的几行 + 行号，让它输出替换后的那几行，调度器落盘；或直接用 `sed` / `perl -i -pe` 这类确定性命令（这类命令根本不需要模型）。
2. **升给 S 档**做最小 diff。

判断标准：**涉及文件 > 150 行时，优先用确定性命令（sed/perl）或升 S 档**，别用弱模型整文件重写。

---

## 3. 难度定级

用 `rubric.md` 的 **9 维 × 0–4** 量表，输出**连续难度分（0–100）**再定档。摘要：

| 维度 | 权重 | 0 分 | 4 分 |
|---|---|---|---|
| D1 可验证性 | ×3 | 完全可机械校验 | 纯主观无判据 |
| D2 上下文半径 | ×2 | 单函数内 | 全仓 / 跨系统 |
| D3 推理深度 | ×3 | 字面替换 | 根因分析 / 推翻假设 |
| D4 工具需求 | ×1 | 纯文本 I/O | 不可逆操作 |
| D5 决策自由度 | ×2 | 路径唯一 | 架构选型 |
| D6 失败代价 | ×2 | 丢弃重来 | 线上 / 静默传播 |
| D7 歧义度 | ×1 | 定义明确 | 自相矛盾 |
| D8 输出规模 | ×1 | 单行 | 跨系统 / 多轮迭代 |
| D9 领域依赖 | ×1 | 通用知识 | 组织专有知识 |

```
难度分 = (D1×3+D2×2+D3×3+D4×1+D5×2+D6×2+D7×1+D8×1+D9×1) ÷ 64 × 100
→ 0-20 L1 ｜ 21-40 L2 ｜ 41-58 L3 ｜ 59-75 L4 ｜ 76-100 L5
→ 再做任务族修正（机械文本 −8 … 线上运维 +12）
→ 最后过 7 道硬门槛，取更高者
```

**路由表里每一行的等级必须附算式**，写不出算式就是拍脑袋。
分值只能取 0–4 整数，越界即重打。细节与 20 个边界案例见 `rubric.md`。

### 3.1 定级复用（批量任务必用）

**结构相同的子任务只算一次 9 维**，其余标注「同 #N 定级」，不要每个都重算。

| # | 子任务 | 难度分 | 等级 | 路由 |
|---|---|---|---|---|
| 1 | 改 `get_user_id` | 12 | L1 | 弱档（算式见此行） |
| 2 | 改 `parse_json_data` | 12 | L1 | **同 #1** |
| … | （共 12 个同类） | 12 | L1 | **同 #1** |

判断"结构相同"：**任务动词一致 + 输入输出形态一致 + 上下文范围一致**。
只有首个要写算式，其余引用即可——12 个改名任务算 12 遍 9 维是纯浪费。

---

## 4. 拆分规则

- 拆到"一次调用能完成"的粒度，按第 2 问选的颗粒度控制数量。
- **拆不开、或拆完还互相纠缠的大泥球** → 整体判 L4/L5 交给强模型，别硬拆，硬拆的合并成本高于省下的钱。
- 太碎也亏：一个只值 200 token 的子任务，走完整 agent 框架要付几千 token。单个子任务低于 ~150 token 就别再拆了。

### 4.1 强档的写操作也要批量

**创建/修改 N 个文件 → 写一个 shell 脚本一次执行完（heredoc 批量），不要 N 次逐个创建或编辑。**

理由和批量派发完全一样：每一次工具调用都是一轮强档推理（实测约 6 秒）。

| 方式 | 轮次 | 实测 |
|---|---|---|
| 逐个创建（❌） | N 轮 | 上一轮测试里，光生成 4 个素材文件就花掉好几分钟 |
| 批量脚本（✅） | **1 轮决策 + 1 次执行** | 一个脚本 `cat > f <<'EOF' ... EOF` 循环建完 |

```bash
# 批量创建文件的形状（强档只写一次脚本，不逐个 edit）
for spec in "string.js:..." "array.js:..." "date.js:..."; do
  f="${spec%%:*}"; cat > "$f" <<'EOF'
  ...文件内容...
EOF
done
```

例外：需要基于前一次结果迭代的（比如测试失败后再改），才逐个来。


---

## 5. 并行规则

- 无依赖边 → 同一并行组，同时启动。有依赖 → 串行。
- **写冲突红线**：同组内禁止两个子任务写同一个文件，各自写 `.scratch/task-<n>.out`，由调度器统一合并。
- **并行度按档位区分**（实测数据，见下表）——统一设 5 是错的，两档的瓶颈完全不同：

| 档 | 并行度上限 | 实测依据 |
|---|---|---|
| **W 弱** | **10** | GLM 并发 5 / 8 / 12 的耗时是 5.64s / 4.93s / 4.97s，**吞吐线性上升、零失败**——12 都还没到瓶颈。设 10 是留安全余量。旧规则设 5 明显太保守。 |
| **S 强** | **3** | v4-pro 实测并发**反而变慢**（限流），必须留余量。 |
- 难度分越高越不该并行（越容易出问题，返工成本高）。L4/L5 一律串行。

### 5.1 批量派发：**所有 W 档子任务必须一次性派发，禁止逐个派发**

> **这是性能的第一杠杆。** 实测：强档（v4-pro）在 agent 框架里一轮决策约 **6 秒**，
> 弱档并行跑 3 个子任务只要 **2.65 秒**。逐个派发时 N 个子任务 = N 轮强档推理；
> 一轮真实任务跑出 7 分钟，绝大部分是强档在"派发→等待→查看→再派发"上空转。

**铁律**：同批的 W 档子任务写进**一个 shell 脚本**，一次执行全部并行跑完。
强档只做两次决策：**① 写派发脚本 → ② 看校验命令的退出码**。

| 派发方式 | 强档决策轮次 | 实测预估 |
|---|---|---|
| 逐个派发（❌ 禁止） | N 个子任务 ≈ N+ 轮 | 5 个子任务 ≈ 7 分钟 |
| 批量派发（✅ 必须） | **2 轮** | 同样任务 ≈ 1–2 分钟 |

- 脚本模板与校验命令见 `weak-channel.md` 的「批量派发脚本」一节。
- 有依赖的子任务留到下一批，别塞进同一批。
- 单个子任务失败 → **只重跑那一个**，不整批重来。

### 5.2 校验下沉到脚本

**禁止让强档用眼睛看弱模型的输出内容**——那要多花一整轮推理，而且不可靠。
一律用确定性命令校验，看**退出码**即可：

| 要验什么 | 用这个 |
|---|---|
| 输出非空 | `[ -s .scratch/task-N.out ]` |
| JS / Python 能否编译 | `node --check` / `python3 -m py_compile` |
| 测试 | `node --test` / `pytest -q` |
| 改名是否彻底 | `grep -c '旧名' *.js` 应为 0 |
| 注释覆盖是否够 | `grep -c '/\*\*' f.js` 对比函数数 |

模型只做两件事：**选校验命令**、**读结果**。内容对不对，交给命令判断。

### 5.3 预热探测（canary）—— 批量派发前必做

> **上一轮真实教训**：它跑了好几分钟才发现弱模型通道不通（密钥读不到），整批工作白做。

批量派发**之前**先发一个最小探针，确认通道是通的：

```bash
# canary：1 次调用，几乎不花 token
if ! run_weak "回复两个字：OK" > /dev/null 2>&1; then
  echo "CANARY_FAIL：弱模型通道不通 → 本批直接升档给强档，不批量派发"
  exit 1
fi
```

| 探测结果 | 动作 |
|---|---|
| 通、快（< 5s） | 正常批量派发 |
| 通、慢（> 15s） | 降低并发度（10 → 5）再派发 |
| 不通 | **直接升档给强档**，别浪费整批 |

代价：一次调用（约 1 秒、几十 token）。收益：避免整批返工。

### 5.4 合并也脚本化

弱模型结果落在 `.scratch/task-N.out` 后，**合并这一步不要靠强档逐个读文件、用眼睛看内容**——那又是 N 次读取 + 至少一轮推理。

```bash
# 一次性合并 + 校验，强档只看退出码
python3 merge.py .scratch/*.out --into src/  &&  node --test
echo "退出码=$?"
```

| | 逐个合并（❌） | 脚本合并（✅） |
|---|---|---|
| 强档动作 | 读 N 个文件 + 判断内容 | 写 1 次脚本 + 读 1 个退出码 |
| 轮次 | 1–3 轮 | **1 轮** |
| 可靠性 | 会看走眼 | 确定性 |
| 重跑 | 麻烦 | 幂等 |

**例外**：需要语义判断的合并（两段逻辑冲突要取舍、输出格式不规范需理解后再整合）才让模型来做。
结构化、可预测的合并一律走脚本。

### 5.5 派发执行清单（逐项打勾，缺一项即违规）

> 实测发现：规则散落在正文里，模型**容易漏掉 canary 和收尾报告**。
> 列成清单、逐项打勾，遵循率明显更高。

**派发前**
- [ ] **用 `prompts.md` 的模板写 prompt**（改名/补注释/写测试/格式转换/翻译），
      不要临场现写——实测：不列进清单就会漏，现写的 prompt 是弱模型出错主因
- [ ] **canary 探测**：先发最小探针确认弱档通道；不通 → 直接升档，不批量派发
- [ ] 派发脚本**自包含**（密钥回退 + `run_weak()` 都写进脚本，不要写"此处省略"）

**执行中**
- [ ] 一次并行跑完 + `wait`，不逐个派发
- [ ] 脚本化校验（看**退出码**，不读内容）
- [ ] **回译校验**（可逆任务必做）：`tools/roundtrip.py`，零 token，专抓"形状对内容错"
- [ ] `tools/merge.py` 合并落盘（不是逐个读文件）
- [ ] **抽检 N 条**（≥10%，0 条要写正当理由）

**收尾**
- [ ] **输出【收尾报告】**：子任务数 / 强·中·弱分布 / 并行批数 / 兜底次数

工具：
- `tools/merge.py`：合并落盘（replace / append / insert_after，空文件判失败）
- `tools/roundtrip.py`：**回译校验**，可逆任务必跑，零 token
- `prompts.md`：**Prompt 模板库**，高频任务直接用，别临场现写

派发前先查 `prompts.md` 有没有现成模板——现写的 prompt 是弱模型出错的主要来源。

---

## 6. 兜底机制（弱模型执行失败）

> 这是整套方案最容易翻车的地方。**任何一次兜底都必须记录并在收尾报告里列出，不许静默降级。**

### 6.1 传输层失败（超时 / 5xx / 429 / 连接错误）

| 第几次 | 动作 |
|---|---|
| 第 1 次失败 | 原参数**重试 1 次** |
| 第 2 次失败 | **降为串行**重试 1 次（很多"并发超时"单独跑其实是好的） |
| 第 3 次失败 | 该子任务**升档**到 M；M 也失败 → S 档（我）接手 |
| 整组 ≥50% 失败 | 判定厂商侧故障，**整组降级串行 + 并发度减半**再跑一轮 |
| 仍不行 | 全部转 S 档，并明确告诉用户：外部通道不可用，本轮成本会高 |

### 6.2 内容层失败（有回复，但不合格）

| 情况 | 动作 |
|---|---|
| 格式校验不过 | 原 prompt + 格式纠错提示，重试 1 次 |
| 语义抽检不过 | 升档到 M |
| M 也不过 | S 档亲自做 |
| 输出越界（改了不该改的文件 / 超出声明范围） | **直接判失败**，退回重拆任务，不重试 |

### 6.3 静默失败防护（最重要）

以下情况**一律视为失败**，绝不当"已完成"：

- 返回为空、只有空白、长度异常短
- 返回的是"我无法…"、反问、要求更多信息
- 返回了内容但格式对不上（比如要代码却给了 markdown 围栏包裹的解释）
- 明显在编造（引用了不存在的函数、文件路径）

> 一次静默失败混进结果，比直接失败贵十倍——它会在最后合并时才炸，而且不知道是哪个子任务出的问题。

### 6.4 止损

- 单个子任务最多 **3 次**尝试（含重试）。
- 单轮**兜底率 > 30%** → 说明量表对当前项目偏乐观，该类任务整体上调一档。
- 兜底次数超过子任务总数的 **50%** → 停止派发，向用户报告，不再硬冲。

---

## 7. 质量闸门

每个子任务产出后先过闸门，不过就按 6.2 处理：

| 闸门 | 适用 | 判定 |
|---|---|---|
| 格式校验 | L1/L2 | 正则 / JSON schema / 能否被解析 |
| 编译 / lint | L2/L3 | 改动能否通过构建 |
| 单测 | L2/L3/L4 | 相关用例是否全绿 |
| diff 边界 | 全部 | 改动是否超出子任务声明的范围（越界=拆得不好，退回重拆） |
| 强模型抽检 | **所有弱档产出，必做** | 见下方 §7.1，不是可选项 |

### 7.1 抽检（必做，不是建议）

> **实测教训**：上一轮收尾报告写"抽检 0 条（L1 全机械校验）"——用一句"机械校验够了"把抽检整个免掉了。
> 机械校验只能证明**形状对**，证明不了**内容对**。`writeTempFILE` 那种错（该是 `writeTempFile`）
> 语法能过、正则能过，只有内容校验或回译能抓住。

- **弱档产出必须抽检**：难度分 20 以下 ≥10%，40 以上 ≥30%。
- 抽检由强档做：随机挑 N 条，**判断内容是否真的正确**（不只是格式）。
- 收尾报告必须写「抽检 N 条，合格 M 条」。
- **抽检 0 条必须写明正当理由**（例如"全部通过回译校验且零差异"），
  不接受"机械校验够了"这种免检说法。

---


## 8. 收尾报告（每轮必输出）

```
子任务 5 ｜ 强 1 ｜ 中 1 ｜ 弱 3 ｜ 弱模型占比 60%
并行组 3 个，理论串行 5 次调用 → 实际 3 批
兜底 1 次（20%）：#3 格式校验不过 → 重试后通过
抽检 1 条，合格
```

没有这行汇总等于没做调度——成本优化必须可度量。

---

## 9. 附录 A：W 档调用模板（直连 HTTP，不经 agent 框架）

> 完整代码见 [`weak-channel.md`](./weak-channel.md)，派发时才读。

关键三条：
1. `export WEAK_NO_THINK=1` → 请求体加 `"thinking":{"type":"disabled"}`。**实测同一任务 242 token → 28 token（省 88%），输出不变。换非推理模型时取消。**
2. prompt 必须走环境变量传给 python 拼 JSON，直接拼进 `-d` 会被引号/换行搞坏。
3. 失败必须 `sys.exit('WEAK_CALL_FAILED: ...')` 显式报错，不许静默返回空。

---

## 10. 附录 B：环境说明

- 本规则跑在 **ChatGPT 桌面端的 Codex** 里。桌面端 / CLI / IDE 扩展共享 `~/.codex/` 下的 `config.toml` 和 `auth.json`，三端配置通用。
- **指令三级层级**：全局 `~/.codex/AGENTS.md`（入口，每轮加载）→ 仓库根 `AGENTS.md` → 子目录。桌面端对话若不在项目目录，只有全局层生效——这是"规则没被加载"的常见原因。
- 桌面端从 Dock 启动读不到 shell 环境变量，所以密钥写在 `config.toml` 的 `experimental_bearer_token`（chmod 600），而非 `env_key`。

---

## 11. 反模式（看到就停下）

- 新会话第一条回复不输出开工配置 —— 用户看不到模式入口，整套协议等于不存在。
- 直接开干不展示方案 —— 用户失去否决权，这是本模式存在的意义。
- 让弱模型去改代码文件 —— 它没工具，且失败不可控。
- 弱模型没关思考 —— 88% 的 token 会烧在"想"一个机械任务上。
- 弱模型的输入里没贴全上下文，指望它"自己找" —— 它找不到，只会编。
- 超时了当空结果处理 —— 静默失败的代价比明失败高一个数量级。
- 拆到"改一行"也算一个子任务 —— 调用开销大于省下的 token。
- 兜底了不报告 —— 那成本账就是假的。
