# prompts.md — 弱模型 Prompt 模板库

> 派发 W 档时才读。高频任务**直接用模板，不要临场现写**。
>
> 为什么需要模板：实测中同一类任务每次现写 prompt，弱模型的理解偏差全靠运气。
> 模板里带**正例 + 反例**，能显著减少它"发挥"——反例尤其重要，光说"不要解释"没用，
> 要让它看见什么是错的。

## 通用骨架（所有模板都按这个结构）

```
<一句话任务，用动词开头：把…改成… / 为…写…>
<输入，必须贴全，不能让它自己找>

输出要求：
- <格式约束，写死>
- 只输出<产物>，不要解释、不要 markdown 围栏、不要多余标点

正确输出示例：
<正例>

错误输出示例（这些都不行）：
<反例 1>
<反例 2>
```

三条铁律：
1. **输入必须贴全**——弱模型没有文件系统，它看不到你的代码。
2. **输出格式写死**——"只输出 X，不要 Y"，越具体越好。
3. **必须给反例**——只说"不要解释"效果差，让它看见错误长什么样。

---

## 1. 标识符改名（snake_case → camelCase）

```
把下面的标识符改成驼峰命名（camelCase）。

输入：
{{INPUT_LIST}}

输出要求：
- 只输出改后的标识符本身，一行一个
- 不要解释、不要代码围栏、不要引号、不要编号

正确输出示例：
getUserId

错误输出示例：
`getUserId`        ← 不要反引号
"改后是 getUserId" ← 不要解释
1. getUserId       ← 不要编号
```

**派完后必须跑回译校验**（`tools/roundtrip.py`），零成本且能抓住 `writeTempFILE` 这类"形状对、内容错"。

## 2. 补文档注释（JSDoc / docstring）

```
为下面的 {{LANG}} 函数写一行文档注释。

代码：
{{CODE}}

输出要求：
- 只输出注释本身，不要重复代码，不要解释
- 一行，说明这个函数做什么（不是怎么做的）
- {{STYLE}} 格式（JS 用 /** ... */，Python 用 """..."""）
- 用英文

正确输出示例：
/** Convert a string to camelCase. */

错误输出示例：
这个函数把字符串转成驼峰。     ← 不要中文，不要"这个函数"开头
/** @param s the string */      ← 除非明确要求，不要展开参数
```

## 3. 写单元测试

```
为下面的函数写单元测试，框架用 {{FRAMEWORK}}。

代码：
{{CODE}}

输出要求：
- 只输出测试代码本身，不要解释、不要围栏
- 覆盖：正常输入、边界（空值/极值）、错误输入
- 每个用例带一句断言说明

正确输出示例：
test('toCamelCase converts snake_case', () => {
  expect(toCamelCase('get_user_id')).toBe('getUserId');
});

错误输出示例：
// 这里是测试代码...      ← 不要解释性开头
describe('tests', ...)    ← 除非要求，别套多余层级
```

## 4. 格式转换（JSON / YAML / 表格）

```
把下面的 {{FROM}} 转换成 {{TO}}。

输入：
{{INPUT}}

输出要求：
- 只输出转换结果，不要解释、不要围栏
- 保持字段顺序与原数据一致
- 不要新增或删除任何字段

正确输出示例：
{{GOOD_EXAMPLE}}

错误输出示例：
转换结果如下：...   ← 不要前言
（缺字段）          ← 字段必须一一对应
```

## 5. 翻译

```
把下面这段{{文本类型}}翻译成{{目标语言}}。

原文：
{{TEXT}}

输出要求：
- 只输出译文，不要解释、不要双语对照
- 保留代码标识符、路径、变量名原文不译
- 保留原有的换行与结构

错误输出示例：
"翻译如下："      ← 不要前言
把 getUserId 改成 获取用户ID  ← 代码标识符不译
```

---

## 使用方式

1. 从上面挑对应模板，替换 `{{变量}}`。
2. 如果任务不在模板里，按**通用骨架**自己写一个，但**必须包含反例**。
3. 新的高频模板跑顺了就补进这个文件——模板库是越用越省的资产。
