# rubric.md — 难度定级量表 v2（9 维 × 5 档）

> 配套 `agent.md`。只在定级这一步加载。
> **v2 改了什么**：v1 每个维度只有 0/1/2 三档，实战中大量任务挤在同一档里分不开（"修个 bug"和"加个小特性"都是 L3，但成本差一倍）。
> v2 把刻度扩到 **0–4 五档**，维度从 7 个增加到 **9 个**，并输出**连续难度分（0–100）**而不是粗档位——同档内也能比较。

---

## 一、九个评分维度

每个维度给 **0 / 1 / 2 / 3 / 4** 分。锚点描述就是判据，不要凭感觉。

### D1 可验证性（×3）
输出的对错，能不靠人来判断吗？

| 分 | 判据 |
|---|---|
| 0 | 完全可机械校验：正则、JSON schema、编译、lint、单测、diff 行数 |
| 1 | 大部分可机械校验，少量边角需要人看一眼 |
| 2 | 能跑测试但覆盖不全，或需抽样人工确认 |
| 3 | 主要靠人工判断，只有部分客观标准 |
| 4 | 纯主观，没有任何自动化判据（"这个设计好不好"） |

### D2 上下文半径（×2）
要看到多大范围？

| 分 | 判据 |
|---|---|
| 0 | 单个函数内 |
| 1 | 单文件内多处 |
| 2 | 单模块内，2–5 个文件 |
| 3 | 跨模块 / 跨层（前后端、服务间） |
| 4 | 全仓 / 跨仓库 / 跨系统 |

### D3 推理深度（×3）
要往深想几步？

| 分 | 判据 |
|---|---|
| 0 | 字面替换、套模板，不需要理解语义 |
| 1 | 单步逻辑 |
| 2 | 2–3 步因果链 |
| 3 | 多步推理，需建立跨模块语义关联 |
| 4 | 根因分析、需推翻已有假设、长链条推理 |

### D4 工具需求（×1）

| 分 | 判据 |
|---|---|
| 0 | 纯文本 I/O，输入已经贴全 |
| 1 | 需要读代码库（只读） |
| 2 | 读写单个文件 |
| 3 | 多文件写入 / 执行命令 / 跑测试 |
| 4 | 执行不可逆操作（部署、迁移、发布、改数据） |

### D5 决策自由度（×2）

| 分 | 判据 |
|---|---|
| 0 | 只有一条路，没有取舍 |
| 1 | 命名 / 风格级取舍 |
| 2 | 实现方案取舍，影响不超出单个模块 |
| 3 | 接口 / 数据契约级取舍 |
| 4 | 架构 / 技术选型级取舍 |

### D6 失败代价（×2）

| 分 | 判据 |
|---|---|
| 0 | 丢弃重来即可 |
| 1 | 一条命令就能回滚 |
| 2 | 需人工排查，但影响锁在改动范围内 |
| 3 | 影响他人 / 需要协调回滚 |
| 4 | 线上 / 数据 / 不可逆 / **错误会静默传播**（当时看不出，事后才炸） |

### D7 歧义度（×1）

| 分 | 判据 |
|---|---|
| 0 | 输入输出都被明确定义 |
| 1 | 有小处需推断，不影响主干 |
| 2 | 多处需推断，但主干清楚 |
| 3 | 需求模糊，必须先澄清 |
| 4 | 需求自相矛盾 / 目标本身不明 |

### D8 输出规模与结构（×1）　*v2 新增*

| 分 | 判据 |
|---|---|
| 0 | 单行 / 单词 / 单一值 |
| 1 | 单文件片段（< 50 行） |
| 2 | 单文件完整（50–300 行） |
| 3 | 多文件（> 300 行） |
| 4 | 跨系统产出，或需要持续多轮迭代才能完成 |

### D9 领域依赖（×1）　*v2 新增*
弱模型最怕的不是难，是**它不知道你们项目的私有约定**。

| 分 | 判据 |
|---|---|
| 0 | 通用知识，任何模型都会 |
| 1 | 常见框架 / 库的公共常识 |
| 2 | 项目内约定，且有文档可查 |
| 3 | 项目内隐性知识，无文档，必须读代码推断 |
| 4 | 组织 / 业务专有知识，代码里根本没有 |

> ⚠️ **v2.1 实测修正：D9 高 ≠ 必须强档。**
> 实测两题——"按既有代码风格新增函数"（D9=3）和"匹配跨模块 handler 接口"（D2=3）——
> **弱档都做对了，而且比强档快 4–8 倍**（3s vs 4s、6s vs 26s）。
> 原因是这两题的上下文都能**在单次调用的 prompt 里贴全**。
>
> 所以判 D9 时真正的分水岭是：**所需上下文能否一次贴完**。
> - 能贴完（哪怕内容多）→ 弱档可行，别急着升档
> - 需要它**自己探索代码库**、或多个文件互相参照才能定下来 → 必须强档
>
> 换句话说：弱模型怕的不是"要理解上下文"，而是"要自己去找上下文"。

---

## 二、算分

```
加权总分 = D1×3 + D2×2 + D3×3 + D4×1 + D5×2 + D6×2 + D7×1 + D8×1 + D9×1
         （每维 0–4，权重合计 16，满分 64）

难度分（0–100） = 加权总分 ÷ 64 × 100
```

**难度分 → 等级**：

| 难度分 | 等级 | 路由 |
|---|---|---|
| 0 – 20 | **L1** | W 弱 |
| 21 – 40 | **L2** | W 弱（可抽检） |
| 41 – 58 | **L3** | M 中 |
| 59 – 75 | **L4** | S 强 |
| 76 – 100 | **L5** | S 强（且需先澄清） |

连续分不只是好看：**同档内分数越高，抽检比例越高、越不该并行**。

---

## 三、任务族修正（v2 新增）

不同任务类型对弱模型的友好度差很多。定级后按类型在**难度分**上做修正，再重新定档：

| 任务族 | 修正 | 说明 |
|---|---|---|
| 机械文本变换（改名 / 格式化 / 翻译 / 格式转换） | **−8** | 弱模型的强项，别高估难度 |
| 样板代码 / 配置生成 | **−4** | 套模板 |
| 测试编写 | **−3** | 有明确结构可循 |
| 常规编码 | **0** | 基准 |
| 文档写作（需理解项目） | **+3** | 容易写错细节 |
| 数据分析 / 建模 | **+6** | 容易算对过程、算错口径 |
| 开放研究 / 调研 | **+10** | 弱模型容易一本正经地编 |
| 线上运维 / 故障排查 | **+12** | 失败代价天然高 |

---

## 四、硬门槛（7 道）

命中任一条，等级下限被抬高。**最终等级 = max(难度分对应等级, 门槛等级)**。

| 门槛 | 条件 | 下限 |
|---|---|---|
| G1 | D4 ≥ 3（多文件写入 / 执行命令） | ≥ L3 |
| G2 | D4 = 4（不可逆操作） | ≥ L4 |
| G3 | D6 ≥ 3（影响他人 / 静默传播） | ≥ L4 |
| G4 | D2 = 4 且 D3 ≥ 3（跨系统且深推理） | ≥ L5 |
| G5 | D5 = 4（架构选型） | ≥ L4 |
| G6 | D7 ≥ 3（需求模糊） | **先澄清再派发** |
| G7 | D9 = 4（需组织专有知识） | ≥ L4 |

---

## 五、边界裁定案例（20 个，v2 扩充）

| 任务 | 直觉定级 | 实际 | 关键理由 |
|---|---|---|---|
| 全仓 `get_user` → `getUser` | L3（文件多） | **L1** | D3=0 D8=1，文件多≠难；再 −8 文本修正 |
| 给模块导出函数补 JSDoc | L2 | **L1** | 套模板，D3=0 |
| 把 README 翻译成中文 | L1 | **L1** | 纯文本变换 −8 |
| 写正则提取日志里的 IP | L1 | **L1** | 唯一答案 + 可机械校验 |
| 按给定签名实现函数 | L2 | **L2** | D1=1 D5=0 |
| 为这个函数写单测 | L2 | **L2** | 跑测试即知对错，−3 |
| Python 函数翻译成 TS | L2 | **L2** | D1=0（有测试可验证） |
| 空数组导致崩溃，修一下 | L2 | **L2 或 L3** | 看 D2：单文件→L2，需追调用方→L3 |
| 把 200 行 god function 拆小 | L4 | **L3** | 取舍在单文件内，但 D4=2 → G1 抬到 L3 |
| 单模块重构 | L3 | **L3** | D2=2 D5=2 |
| 给既有模块加一个特性 | L3 | **L3** | 常规编码 +0 |
| 跨文件修 bug | L3 | **L3** | D2=2 D3=2 |
| 新增 API 端点（含前端+文档） | L4 | **L4** | D2=3 跨层 |
| 改接口契约并同步所有调用方 | L4 | **L4** | D5=3 + D2=3 |
| 引入新依赖并改造接入点 | L4 | **L4** | D5=3 |
| 查询 3 秒，优化一下 | L3 | **L4** | D3=4 根因分析 → 分数抬进 L4 |
| users 表主键 int→uuid | L4 | **L5** | D4=3 + D6=4（G3）+ D2=3 |
| 写项目技术方案文档 | L3 | **L3** | 文档 +3，D9=2 |
| 排查线上偶发 502 | L4 | **L5** | 运维 +12，D7=3 先澄清 |
| 这套代码该不该上微服务 | L4 | **L5** | D5=4（G5）+ D1=4 无自动判据 |

---

## 六、编码任务的路由判据（v2.1 实测标定）

> 两轮共 9 道编程题，弱档与强档正面对撞，生成的代码直接挂断言用 node 跑——跑过才算对。

### 实测数据

| 任务 | 弱档 GLM（关思考） | 强档 ChatGPT |
|---|---|---|
| 回文判断 | ✅ 3.6s / 128tk | ✅ 9.7s / 799tk |
| 防抖函数 | ✅ 3.2s / 113tk | ✅ 6.0s / 523tk |
| 二分查找 | ✅ 3.7s / 153tk | ✅ 12.1s / 989tk |
| 深拷贝 | ✅ 4.0s / 168tk | ✅ 42.6s / 2791tk |
| LRU 缓存 | ✅ 4s / 191tk | ✅ 28s / 1979tk |
| 限流器（异步） | ✅ 8s / 344tk | **❌ 47s / 3356tk** |
| 状态机 | **❌ 7s / 299tk** | ✅ 34s / 2262tk |
| 既有风格（D9=3） | ✅ 3s / 283tk | ✅ 4s / 593tk |
| 跨模块接口（D2=3） | ✅ 6s / 400tk | ✅ 26s / 2042tk |
| **合计** | **8/9，86s，1979tk** | **8/9，229s，15434tk** |

**正确率持平（都是 8/9），弱档快 2.7 倍、省 87%。**

### 判据（定级后套用，决定能不能下放弱档）

| 条件 | 路由 |
|---|---|
| 单函数 / 单类，输入输出明确，**上下文能一次贴全** | **弱档**（哪怕难度分算到 L3） |
| 需要**同时改多个文件** | 强档 |
| 需要设计接口 / 架构取舍（D5 ≥ 3） | 强档 |
| 涉及异步、并发、状态机这类**易错语义** | 弱档可试，**但必须跑测试校验** |
| 上下文需要它**自己探索代码库** | 强档 |

### 三条推论

1. **"写代码"本身不是升档理由**——单函数实现，弱档和强档正确率一样。
2. **真正该升档的是**：跨文件协调、设计决策、上下文无法贴全。
3. **强档也不是免检的**——实测它在限流器上翻车（47 秒还写错）。
   **任何档位的产出都要跑测试**，不能因为"是强档写的"就放行。


---

## 七、定级输出格式（强制）

```
#3  为 AuthService 补单测
    D1=1 D2=2 D3=2 D4=2 D5=0 D6=1 D7=0 D8=2 D9=2
    加权 = 1×3+2×2+2×3+2×1+0+1×2+0+2×1+2×1 = 21
    难度分 = 21/64×100 = 33 → L2
    任务族修正：测试编写 −3 → 30 → 仍 L2
    Gate: D4=2 未触发（G1 要求 ≥3）
    最终 = L2（难度分 30）→ 路由 W 档，抽检比例 20%
```

写不出算式的定级 = 拍脑袋，重打。

---

## 八、校准

- 每个维度**只有 0–4 五个整数分值**，没有 0.5、没有 5、没有"大概 2 点多"。
- 出现越界分值 → 定级无效，重打。
- 拿不准 → **取高的那一档**。定高只是多花钱，定低要返工。
- 跑满 20 个子任务后，用实际**兜底率**回填校准：兜底率 > 30% 说明这张表对当前项目偏乐观，整体上调一档。
